{
  "name": "ts-codegen",
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "build": "tsc --project tsconfig.build.json",
    "fix": "gts fix",
    "prepare": "yarn run build",
    "test": "yarn jest"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "^14.18.0",
    "jest": "^27.4.5",
    "sijiaoh-gts": "^0.6.1",
    "ts-jest": "^27.1.1",
    "typescript": "^4.5.4"
  }
}
